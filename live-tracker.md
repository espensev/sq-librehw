# Campaign tracker

This tracker records source-campaign work. It does not describe or authorize a
live deployment, and a `Done` row is agent-level completion, not campaign
acceptance. Acceptance lives in `docs/campaign-history.md`.

## Plan-001 — fixture-only Avalonia sensor explorer

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| AVSPIKE-001 | Done | agent-a | package/project bootstrap and contracts | Fixture-only Avalonia explorer | Restored three isolated projects, passed the Core Release build, froze loader/snapshot interfaces, and passed 114/114 release-system assertions. Launch candidate creation internally verified clean/promotable state, but independent dual-shell current-source verification was not recorded before launch. |
| AVSPIKE-002 | Done | agent-b | bounded parser and fixtures | Fixture-only Avalonia explorer | Enforced every byte/depth/node/child/string/identity bound with atomic immutable projection; 49/49 parser and replacement cases passed. A reliable Windows ACL-denied test remains unavailable, although typed catches exist. A non-cooperative caller stream can outlive logical supersession; the app uses a bounded `FileStream`, and view-model request identity prevents stale publication. |
| AVSPIKE-003 | Done | agent-c | Avalonia shell and headless UI | Fixture-only Avalonia explorer | Covered explicit states, accessible labels, keyboard focus/navigation, cancellation, and safe errors; 19/19 UI/headless cases passed. Known `AVLN3001` source-build warning remains; attended smoke is pending. |
| AVSPIKE-004 | Done | agent-d | integration, verification, docs | Fixture-only Avalonia explorer | Added seven real parser-to-view-model cases (68 pre-D, 75/75 final) and passed 10/10 source/regression gates. Manager candidate `0.9.6-20260730-210528120-b466837` passed dual-shell exact-source verification and both package inventories contained zero Avalonia/spike entries. Attended smoke remains pending; read-only polling is worth a separate spec only. |

## Plan-002 — non-deploying CI gates and campaign history contract

Agent I is the only writer of this file. Every other Plan-002 agent returned its
row text in its result payload rather than editing here, because
`docs/README.md, live-tracker.md, data/plans/` is a declared conflict zone.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| CIGATE-001 | Done | agent-e | non-deploying gate runner | Non-deploying CI gates | `eng/ci/Invoke-LhmGates.ps1` reads gate commands from `.codex/skills/project.toml` at run time and hard-codes none. Every configured gate must be explicitly classified or the run fails closed; a 27-pattern deny-list is applied to each resolved command immediately before execution, so a later configuration edit that adds a deploying command is still refused. Verified `-List`, `-All -DryRun`, single-gate, unknown-gate, excluded-gate, unclassified-gate, deny-list refusal, failing-gate, and `-FailFast` paths under both PowerShell engines. The full `-All` sweep passed 8/8 with the live process, root task, and CSV untouched. |
| CIGATE-002 | Done | agent-f | dispatcher and gate-runner suite | Non-deploying CI gates | `eng/ci/Test-LhmCiGates.ps1` runs every `eng/ci/tests/Test-*.ps1` in a child process of the same engine and exits 0 on an empty test root, so the `ci-gates` gate cannot fail on an unpopulated suite. `Test-GateRunner.ps1` covers all eleven required cases. Two initially passed for the wrong reason: `Set-Content -Encoding UTF8` writes a BOM that `tomllib` rejects, so fixtures failed to parse and the runner exited 2 before reaching the behavior under test, and a double-quoted Python key was stripped by native argument passing, leaving an empty expected set. Both are now asserted specifically rather than by bare exit code. |
| CIGATE-003 | Done | agent-g | read-only workflow delegation | Non-deploying CI gates | `.github/workflows/non-deploying-gates.yml` declares `permissions: contents: read` and nothing else, references no repository secret and no deployment, publish, or cloud-login action, and its single gate step delegates to the runner rather than duplicating any gate command. Eleven assertions pass. The three safety-critical ones were negative-tested: broadened permissions, an inline `dotnet test`, and an injected secret reference each failed the suite, after which the workflow was restored byte-identical. The workflow has never executed on a hosted runner because `origin` is deliberately unpushed; it is verified by YAML parse and structural inspection only. |
| CIGATE-004 | Done | agent-h | campaign-history transition contract | Non-deploying CI gates | Separated two axes that were previously conflated: plan lifecycle status answers whether the tooling registered and ran a campaign, ledger state answers whether a person accepted it. `executed` arrives the moment a plan is registered, with every criterion open, so no acceptance rule may key on plan status. `docs/campaign-history.md` adds four ledger states with named owners, a rule barring `accepted` or `closed` while any criterion is open, and five mandatory waiver fields. 31/31 campaign-control tests pass, nineteen new, eleven of which prove the rules reject broken fixtures. Plan-001 stays `partial` with criterion 9 open. Open question recorded there: criterion 1's pre-launch ordering clause was not satisfied, though its substance was verified afterwards. |
| CIGATE-005 | Done | agent-i | control-plane wiring and docs | Non-deploying CI gates | Registered the `ci` module, the `ci-gates` build gate, four smart-test mappings, and two new conflict zones in `.codex/skills/project.toml`, and claimed `scripts/Test-AvaloniaSpike.ps1` under `avalonia-tests` — the only real module-ownership gap behind the standing unassigned-files analyzer warning. Closed both open roadmap Phase 1 items, recorded the deliberate early adoption of the Phase 2 `eng/ci` path and the campaign ownership rules, and folded every still-live item from `docs/HANDOFF.md` into the roadmap and current-state docs. Retirement was initially held because `AGENTS.md` was outside this agent's ownership set; the maintainer then authorized that edit, so `AGENTS.md` now points at the roadmap and `docs/HANDOFF.md` was removed with `git rm`, the original staying recoverable at `HEAD`. |
| CIGATE-006 | Done | maintainer | post-campaign gap closure | Non-deploying CI gates | Three gaps reported by agents E, F, and I were closed under maintainer authorization. `AGENTS.md` source-of-truth map now names the roadmap as the continuation checkpoint and adds `docs/campaign-history.md` and `eng/ci/README.md`; `docs/HANDOFF.md` deleted. `Clear-LhmRepositoryBuildOutputs.ps1` gained the six Avalonia spike `bin`/`obj` paths, keeping its deliberately explicit list and passing its reparse-point and runtime-state guard suite unchanged. The analyzer `exclude-globs` gained root-relative `bin/**`, `obj/**`, `__pycache__/**`, and `.git/**`: the configured list replaces the defaults rather than extending them, and `fnmatch` gives `**` no special meaning, so `**/bin/**` never matched a repository-root `bin/`. Unassigned inventory fell 112 to 103 with zero build output remaining; the residue is three cross-cutting entry points already declared in `[smart-test.cross-cutting]`. |

## Plan-003 — Avalonia fixture explorer relocation

Agent N is the only writer of this file for Plan-003; agents J-M returned their
row text in their result payloads.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| AVMOVE-001 | Done | agent-j | move spike projects | Avalonia fixture explorer relocation | Moved the three spike projects and the `.slnx` under `experiments/avalonia-fixture-explorer/` with `git mv`; the cluster's internal relative references resolved unchanged and the moved solution built clean in Release (0 errors). No inherited product root changed. |
| AVMOVE-002 | Done | agent-k | rewire spike config | Avalonia fixture explorer relocation | Rewired `.codex/skills/project.toml` (modules, smart-test mappings, gate commands, conflict zone, cross-cutting), relocated `Test-AvaloniaSpike.ps1` into the experiment root with fixed repository-root resolution, and updated the six Avalonia `bin`/`obj` paths in `Clear-LhmRepositoryBuildOutputs.ps1`. `Invoke-LhmGates.ps1` stayed byte-identical, proving configuration-only rewiring. |
| AVMOVE-003 | Done | agent-l | stale-reference gate | Avalonia fixture explorer relocation | Added the permanent `eng/ci/tests/Test-NoStaleReferences.ps1` with a data-driven move map and a centralized allow-list for immutable historical evidence. The CI dispatcher now discovers three test scripts. |
| AVMOVE-004 | Done | agent-m | spike-move docs | Avalonia fixture explorer relocation | Updated current path claims in `docs/README.md` and the Avalonia feature spec, and the one spike path in the structural discovery audit, so no current document references the old location. |
| AVMOVE-005 | Done | agent-n | spike-move close | Avalonia fixture explorer relocation | Added the Plan-003 ledger row and criterion evidence to `docs/campaign-history.md` without automatic acceptance, recorded the maintainer's honest deferral of the plan-001 attended smoke, closed the Phase 2 Avalonia roadmap item, and removed the completed Plan-003 section from the backlog. |

## Plan-004 — operations taxonomy

Agent U is the only writer of this file for Plan-004; agents O-T returned their
row text in their result payloads.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| AVOPS-001 | Done | agent-o | move candidate ops | Operations taxonomy | Moved `ops/release/` to `ops/candidate/` with `git mv` (4 files, history preserved) and updated the synthetic `ops\release` mirror in `Test-LhmReleaseSystem.ps1` to `ops\candidate`. |
| AVOPS-002 | Done | agent-p | dissolve local-release | Operations taxonomy | Dissolved `scripts/local-release/` and relocated `ops/local-release/` (9 files to `ops/deploy/snd-desk/`), moved `Clear-LhmRepositoryBuildOutputs.ps1` to `eng/`, and fixed every internal `$PSScriptRoot`/repo-root/cleanup reference broken by the split. |
| AVOPS-003 | Done | agent-r | rewire config and gates | Operations taxonomy | Rewired `.codex/skills/project.toml` (modules, mappings, 4 gate commands), the spike gate's `ops\release` reference to `ops\candidate`, and extended the permanent stale-reference gate with the ops move-map and dissolved-path checks. `Invoke-LhmGates.ps1` stayed byte-identical. |
| AVOPS-004 | Done | agent-s | verify fail-closed | Operations taxonomy | Verified the relocated peer-safe fixture passes under `pwsh` (proving fail-closed guards hold on snd-host), the content-based deny-list still refuses deploying commands, and the extended stale-reference gate passes. |
| AVOPS-005 | Done | agent-t | ops-move docs | Operations taxonomy | Updated 45 path claims across 9 doc/eng files so no current document references a dissolved ops path; the stale-reference gate passes. |
| AVOPS-006 | Done | agent-u | ops-move close | Operations taxonomy | Added the Plan-004 ledger row and criterion evidence to `docs/campaign-history.md` without automatic acceptance, closed the Phase 2 operations-taxonomy roadmap item, advanced the backlog current position, and removed the completed Plan-004 section. |

## Plan-005 — documentation taxonomy

Agent X is the only writer of this file for Plan-005; agents V-W returned their
row text in their result payloads.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| AVDOCS-001 | Done | agent-v | reorganize docs | Documentation taxonomy | Moved 13 feature specs to `docs/features/`, refactor-roadmap and repository-build-output-cleanup to `docs/architecture/`, and retired the 2 completed discovery reviews (findings folded into the roadmap). Tooling-coupled docs stay at `docs/` root. |
| AVDOCS-002 | Done | agent-w | rewire doc references | Documentation taxonomy | Updated 45 path references across AGENTS.md, docs/README, campaign-playbook, campaign-backlog, inter-doc links, the 2 deploy scripts, and extended the stale-reference gate with the doc move-map and a retired/bare-doc-path check. `scripts/task_manager.py` unchanged. |
| AVDOCS-003 | Done | agent-x | docs-move close | Documentation taxonomy | Added the Plan-005 ledger row and criterion evidence to `docs/campaign-history.md` without automatic acceptance, closed the Phase 2 documentation-grouping roadmap item (at its new `docs/architecture/` path), and removed the completed Plan-005 section from the backlog. |

## Plan-006 — verification suite boundaries

Agent AB is the only writer of this file for Plan-006; agents y, z, and aa
returned their row text in their result payloads.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| SUITESPLIT-001 | Done | agent-y | move tests into boundary suites | Verification suite boundaries | Split the flat test project into `LibreHardwareMonitor.Tests.Library`/`.Application`/`.Contracts`/`.Attended` with 28 pure `git mv` renames (all R100, `data.golden.json` byte-identical at blob `05113704a`), retired the old csproj, added the four-project sln graph and the three-suite `LibreHardwareMonitor.Tests.slnf`, and widened both product `InternalsVisibleTo` groups to the four suite names. Per-suite totals 64/64/0, 131/130/1, and 64/64/0 sum to the exact 259/258/1 baseline with the single `LHM_LIVE_CONFIG_PATH` skip in Application; the slnf gate run reproduced 259/258/1 over exactly the three deterministic suites, both WinForms x64 Release targets built 0W/0E, and the Library suite's output contains no `LibreHardwareMonitor.Windows.Forms.dll`. Residuals for agents z/ab: `.codex/skills/project.toml` `test`/`test_fast` still invoke the deleted csproj, `Clear-LhmRepositoryBuildOutputs.ps1` lacks the four suite `bin`/`obj` paths, and ops scripts plus `AGENTS.md`/docs still name the old test paths. |
| SUITEWIRE-001 | Done | agent-z | rewire config and gates | Verification suite boundaries | Rewired every configuration and script reference to the retired flat test project: `project.toml` `test`/`test_fast` and the `winforms-net10` test command now run `LibreHardwareMonitor.Tests.slnf`, the `winforms-tests` module was renamed `dotnet-tests` (no live consumer of the old key), the data.json conflict zone and smart-test mappings point at the Contracts/Library/Application suites with a new WinForms mapping added, `New-LhmRelease.ps1` runs the slnf, `Publish-LibreHardwareMonitor.ps1` loops the three deterministic suites with per-suite OutputPath (a shared OutputPath across a multi-project invocation would collide outputs), `Test-AvaloniaSpike.ps1` runs the slnf and sums per-assembly totals via a new `-Sum` switch, and `Clear-LhmRepositoryBuildOutputs.ps1` lists the eight per-suite `bin`/`obj` paths. Extended `Test-NoStaleReferences.ps1` with 28 per-file move-map entries, dissolved-csproj/new-artifact existence checks, and a widened textual block covering `.csproj/.sln/.slnf/.json`; authored the permanent `Test-SuiteBoundaries.ps1` (13/13) pinning the Attended suite outside deterministic CI. `Invoke-LhmGates.ps1` byte-identical; dispatcher discovers 4 scripts (85/86 assertions). Known intermediate red: the widened stale check flags 12 doc files whose flat old test paths belong to agent ab's docs pass (ownership amended by the maintainer to cover all 12); everything else green. |
| SUITEVERIFY-001 | Done | agent-aa | verify suite boundaries | Verification suite boundaries | Independent verification of the y+z tree: `Invoke-LhmGates.ps1 -All` with sanitized process `PSModulePath` passed 7/8 (exit 1) — the only red is campaign-control's unittest step failing 2/31 assertions because `data/plans/plan-006.json` has no `docs/campaign-history.md` ledger row yet (ab-owned close artifact; all other gates green incl. the slnf winforms-net10 test, the 114-assertion release fixture, and `snd-desk-local-release-fixture` under powershell.exe); the dispatcher discovers 4 scripts under both pwsh 7 and PS 5.1 at 85 passed / 1 failed / 0 skipped with the single documented doc-staleness FAIL naming exactly AGENTS.md, docs/README.md, and 10 docs/features/*.md; per-suite totals 64/64/0 (Library), 131/130/1 (Application, skip = `SettingsPersistenceTests.LiveConfigCopy_LoadsAndCompactsWithinMemoryBudgets`), 64/64/0 (Contracts) sum to the recorded cae786c baseline 259/258/1 and the slnf run reproduces it over exactly the three deterministic assemblies; `data.golden.json` is a pure R100 rename (blob `05113704a`, 2015 bytes both sides, zero content hunks) with DataJsonGoldenTests 2/2 green; the product-root diff is limited to the two csproj InternalsVisibleTo ItemGroups (1 entry → 4 suite names each) plus the sln 39+/9- test-project-graph swap, with Aga.Controls/, Directory.Packages.props, and global.json untouched; the Library suite bin contains no `LibreHardwareMonitor.Windows.Forms.dll` or `Aga.Controls.dll` (Application bin does, as positive control) and its csproj references only LibreHardwareMonitorLib; the independent all-extension tracked-tree grep hits only the 12 ab-owned doc files outside the gate's immutable-history allow-list; and the live five-point proof is unchanged — one process (PID 32244) at the live exe, root task `\LibreHardwareMonitor` Running/267009 with matching action and working directory, `/`, `/data.json`, `/metrics` all HTTP 200 on 192.168.2.5:8080 (socket PID 4, localhost refused), and the 2026-08-01 CSV growing (+254,931 B in ~109 s). |
| SUITECLOSE-001 | Done | agent-ab | docs and campaign close | Verification suite boundaries | Updated all 15 stale path claims across the 12 gate-flagged current docs to the four-suite layout — the baseline/per-doc `dotnet test` commands now run `LibreHardwareMonitor.Tests\LibreHardwareMonitor.Tests.slnf -p:Platform=x64` (`AGENTS.md`, `docs/README.md`, nine feature docs), `docs/README.md` gained the three per-suite csproj commands as focused variants, the `AGENTS.md` golden-master procedure (deletion path and targeted regeneration re-run) and the `feature-host-operator-utilities.md` `DataJsonGoldenTests` filter command point at the Contracts suite, and the spike spec's avoid-list names the suite projects and slnf — after which `Test-NoStaleReferences.ps1` exits 0 (51/51) and the dispatcher reports 4 scripts at 86 passed / 0 failed. Added the Plan-006 ledger row (`executed`/`implemented`, Met 9 / Open 1 / Waived 0) and the verbatim 10-criterion table with y/z/aa evidence — criterion 8 stays honestly `open` because the full `-All` sweep from the final tree is the maintainer's post-close step (aa's 7/8 sole red was the campaign-control unittest requiring this very ledger row; it now passes 31/31) — and mechanically repaired the plan-004/005 ledger row malformation (plan-004's evidence cell had slid onto plan-005's row as a trailing column) plus the plan-005 evidence filename, which pointed at a nonexistent doc. Closed the three Phase 3 suite-split roadmap items with (Plan-006, 2026-08-01) tags leaving characterization open as plan-007, set the checkpoint queue to A1, A2, plan-007, deleted the backlog's plan-006 section, and advanced the current position (next campaign `plan-007`, next agent letter `ac`). Final verification: campaign-control unittest 31/31, stale gate exit 0, dispatcher 4 scripts 86/0/0, preflight ready with only the expected mid-campaign dirty-tree warning, and guarded cleanup (`-WhatIf` reviewed, exactly the 23 configured paths) after which `git clean -ndX` lists only `data/tasks.json` and `data/analysis-cache.json`; `git diff --check` is clean for every ab-owned file and clean tree-wide under `core.whitespace=cr-at-eol` — the literal run exits 2 solely on CR-at-EOL false positives on 40 added CRLF lines of `LibreHardwareMonitor.sln` (byte-proven: file is 116/116 CRLF with zero real trailing whitespace). |

## Plan-007 — characterization tests before extraction

Agent AF is the only writer of campaign truth. AC, AD, and AE returned committed
test-only results from isolated worktrees; AF independently verified the
integrated tree and recorded the result without accepting the campaign.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| CHARLIFE-001 | Done | agent-ac | hardware lifetime characterization | Characterization tests before extraction | Source commit `f019266` integrated as `691c6dc`; one new Library file adds four facts for dynamic-group forwarding/detachment/reverse close, rollback/retry, NVIDIA partial-factory cleanup/lease release, and storage unsubscribe/device close. Focused result 4/4; full Library 68/68. |
| CHARORDER-001 | Done | agent-ad | ordered option/reset and shutdown characterization | Characterization tests before extraction | Source commit `f3ce3f7` integrated as `74592e4`; two existing Application files add four coordinator and four shutdown facts. Focused result 19/19; isolated Application result 139 discovered, 138 passed, and the one existing opt-in skip. |
| CHARSET-001 | Done | agent-ae | settings projection and persistence characterization | Characterization tests before extraction | Source commit `628debb` integrated as `ca5b471`; one new and two existing Application files add exactly eight facts. New-fact result 8/8; AF's settings regression passed 58 with the one existing opt-in skip, and the integrated Application suite was 147 discovered/146 passed/one skipped. |
| CHARCLOSE-001 | Done | agent-af | independent verification and campaign close | Characterization tests before extraction | This row's containing closure commit starts from integrated `ca5b471`; the returned handoff SHA is the non-self-referential commit identifier. AF reproduced focused 4/4, 19/19, and 58 passed/one existing skip; aggregate 279/278/1; both x64 Release builds 0W/0E; final CI 8/8 included; preflight and Git checks green. Read-only live proof found one exact-path process, matching Running task, and three HTTP 200s; CSV grew from 102,429,441 bytes at 12:50:40+01:00 to 103,246,180 at 12:59:02+01:00 (+816,739; +2,323,159 from the supplied before sample). Plan lifecycle remains `executed`; ledger state is `implemented`, not `accepted`. |

## Plan-008 — immutable sensor snapshot and data.json projection

Agent AI is the only writer of campaign truth. AG and AH returned committed
source/test results from isolated worktrees; AI independently verified the
integrated tree and recorded the result without accepting or deploying it.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| SNAPCAP-001 | Done | agent-ag | immutable snapshot capture | Immutable sensor snapshot and data.json projection | Source `dbc7e5dd20b49ceba1f5827d4e2cb3e31158a856` integrated as `cbd1013c431cb745c6f30420755491963197c843`, stable patch-id `9b8bb96abbaffabc9fb75940c91f7deff9029345`; the three owned blobs match. Added detached internal snapshot/value/node types, recursive `Node.SyncRoot` capture, preserved order/hidden nodes/opaque IDs/formatted-raw triples/legacy images, and five focused Application facts. Independent result 5/5; integrated Application 152 discovered/151 passed/one established opt-in skip; WinForms net472 Release 0W/0E. The corrected planning baseline uses `ApplicationModel.Snapshots` after the original namespace caused 17 CS0234 collisions. |
| JSONPROJ-001 | Done | agent-ah | pure data.json projection | Immutable sensor snapshot and data.json projection | Source `215190f93a7494331a5d534e66fbff2b516f120d` integrated as `3a31eb59039a4ce51be642534d8ac9458e9441fd`, stable patch-id `a00a0babf9417a688fe5483d6fba0c644bdd5d24`; the three owned blobs match. Added the ordered pure projector and rewired only `BuildDataJsonObject` capture/projection while preserving `WriteDataJson` and all other HTTP paths. Independent results: projector 3/3, golden 2/2, Sensor plus Prometheus 47/47, Contracts 67/67, web 315/315 plus 18/18, both Release builds 0W/0E; golden blob `05113704acc6fefeb4128004b3f523d876fbcec4` and SHA-256 `BEBDE807A7F0037827E16CFBC1F41707701F2BEE7232385C3D2EAEBD2261D2F3`. |
| SNAPCLOSE-001 | Done | agent-ai | independent verification and campaign close | Immutable sensor snapshot and data.json projection | This row's containing closure commit starts from integrated `3a31eb59039a4ce51be642534d8ac9458e9441fd`; the returned handoff SHA is the non-self-referential commit identifier. AI reproduced all focused counts and exact 287/286/1 aggregate, both WinForms Release targets 0W/0E, Avalonia 75/75, web 315/315 plus 18/18, and final CI 8/8 included. Two read-only live samples retained PID 14876, exact Running task, and three HTTP 200s while CSV grew 118,509,168 to 119,129,453 bytes (+620,285). Phase 4 item 1 is complete/in progress, Plan-009/`aj` is next, A1 remains person-only, and ledger state is `implemented`, not `accepted`; no candidate or live change occurred. |

## Plan-009 — HTTP listener dispatch service extraction

Agent AL is the only writer of campaign truth. AJ and AK returned committed
test/source results from isolated worktrees; AL verified the integrated source
and records `implemented`, never accepted or deployed.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| HTTPWIRE-001 | Done | agent-aj | HTTP wire characterization | HTTP listener dispatch service extraction | Planning baseline `a45790c646ce40be00c627dbcc1f99f681a3f639`; initial source `ad1cc8d7d41502e7a87e8940fbaaf08e2f62f192`, tightened and integrated as `0a35c488e25b1ab61a7849430eed4f970a0e81b3`. One new Contracts file contains exactly six facts for route/status/header, identity/gzip, mutation method/origin, Basic auth, fault closure, and lifecycle. It passed 6/6 before and after extraction; combined wire/lifetime/auth passed 12/12 and Contracts 73/73. HTTP.sys PID-4 listener snapshots were identical around the closure run, with zero new listener. |
| HTTPADAPT-001 | Done | agent-ak | extract listener dispatch adapter | HTTP listener dispatch service extraction | Source `263eef4f3773a1290f5751aa4b6b5afad25738ab` integrated as `b510f47deb06e561a9c17c9767f830282ed8a345`, stable patch-id `69703ba3d34bb0d773c39fa1c3abec495e7763b7`; both owned blobs match. Added the internal sealed net472-compatible listener service and moved only lifecycle/accept/pool/cancel/fault/close/drain mechanics while `HttpServer` retains its facade, auth, routes, mutations, resources, data.json, Prometheus, buffering/gzip, and writers. Wire 6/6, family 12/12, broader 68/68, Contracts 73/73, and both Release builds 0W/0E. |
| HTTPCLOSE-001 | Done | agent-al | independent verification and campaign close | HTTP listener dispatch service extraction | AL reproduced wire 6/6, family 12/12, broader 68/68, Contracts 73/73, aggregate 293/292/1, both WinForms Release targets 0W/0E, Avalonia 75/75 with established AVLN3001, release fixture 114/114, and web 315/315 plus 18/18. Golden blob/SHA remain exact. The post-ledger CI rerun passed all 8 included gates; campaign-control ran 35/35 and dispatcher 86/86. Authoritative-main validation/preflight passed, analyzer was high confidence with only the standing unassigned-inventory warning, and guarded cleanup left only the preserved ignored ledger/cache. All three campaign worktrees and branches were removed after ancestry or patch/blob proof. Read-only live proof retained PID 14876, exact Running task, three HTTP 200s, and CSV +10,848 bytes in six seconds. Plan status is `executed`, ledger `implemented` with all 12 criteria met, not `accepted`; no candidate or live change occurred. |

## Plan-010 — application lifecycle and polling coordinator

Agent AO is the only writer of campaign truth. AM and AN returned committed
source results; AO independently verified the integrated source and records
`implemented`, never accepted or deployed.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| LIFECYCLE-001 | Done | agent-am | lifecycle and polling coordinator | Application lifecycle and polling coordinator | Initial source `6871323b915d1ef005dae4e52b6f9b5d27ee959e`, race fix/final integration `7fc7f77a7c0aac8f8e70f3ea330d266fd9b0e14b`; stable patch IDs `1aaf2562b203fd1b24456e15a83f4b58ba7506fb` and `0188d2b716d3859b9d33d54970f498c92feec7be`. Added only the internal coordinator and its eight-fact test file. Lifecycle passed 8/8, all coordinator families 27/27, Application 159 passed plus one established skip/160, and both Release targets 0W/0E. |
| LIFECYCLEWIRE-001 | Done | agent-an | wire MainForm lifecycle | Application lifecycle and polling coordinator | Source/integration `479c89b78bdac19983632c7729aaea6604d49947`, stable patch ID `3cee8c8bea11c02fb88a5be18d14e64d623ff54e`; owned blobs match integration. Rewired `MainForm` and changed only two comment lines in `UiShutdownCoordinator`. Coordinator families passed 27/27, lifetime 18/18, Application 159+1/160, and both Release targets 0W/0E. PawnIO policy and prompt/install-before-open order remain in `MainForm`; the sequence now starts after the lifecycle barrier rather than synchronously in construction, a reviewed policy-neutral timing deviation that lets shutdown track and drain it safely. |
| LIFECLOSE-001 | Done | agent-ao | independent verification and campaign close | Application lifecycle and polling coordinator | This row's containing closure commit starts from integrated `479c89b78bdac19983632c7729aaea6604d49947`; the returned handoff SHA is the non-self-referential commit identifier. AO reproduced lifecycle 8/8, coordinator families 27/27, lifetime 18/18, Application 159+1/160, Contracts 73/73, aggregate 300+1/301, both WinForms Release targets 0W/0E, Avalonia 75/75 with established AVLN3001, web 315/315 plus 18/18, and final CI 8/8 included. Golden blob/SHA and every protected diff remain exact; campaign tests passed 23/23 and 35/35, affirmative Plan-010 validation passed, analyzer was high confidence, and the generated plan rendered twice byte-identically at 17,217 bytes/SHA-256 `312C170A1261461E3455E387BDCA7098C2E9C6EF395DA8C32FC5EBE417AF2D1B`. Guarded AO cleanup removed only verified in-worktree reproducible outputs and left both ignored previews empty. Read-only live proof at 2026-08-01T21:19:37Z retained PID 11980, exact Running task, three HTTP 200s, and CSV growth of 12,781 bytes in seven seconds. Plan status remains `executed`, ledger `implemented` with all 12 criteria met, Plan-011/agent `ap` is next, A1 remains person-only, and no candidate or live change occurred. |

## Plan-011 — settings projection and persistence coordination

Agent AR is the only writer of campaign truth. AP and AQ returned committed
source results; AR independently verified the integrated source and records
`implemented`, never accepted or deployed.

| ID | Status | Owner | Scope | Issue | Update |
|---|---|---|---|---|---|
| SETPERSIST-001 | Done | agent-ap | settings persistence coordinator | Settings projection and persistence coordination | Source/integration `e58e38dc3ea4bd2fd43f066500546d9377df14da`, stable patch ID `4fd7e4935b7667f551afa2052cce77bbd6779fb1`; coordinator/test blobs `ccccdbd308d5fbc31c2a2a7acaef75fe06a1f1b8` and `bb1f0e8bbf49802ec8416ffaf4b1a81b82e0237b` match integration. Added only the internal delegate-driven coordinator and exactly seven deterministic facts. Coordinator passed 7/7, focused settings 65+1/66, Application 166+1/167, and both Release targets 0W/0E; barrier-backed ordering proves final save persists the latest projection without a hang. |
| SETWIRE-001 | Done | agent-aq | wire MainForm settings | Settings projection and persistence coordination | Source/integration `8f68c09c0a89bcd0424e88c97d054ba7a390ff97`, stable patch ID `32b27bfa0cc0431f0298f345cc560a57a9648140`, MainForm blob `13d2a728857fb647f9a426aaa66b6dae4635d2fb`. Modified only `MainForm` to compose the coordinator while retaining exact UI/server projection order, keys/values, autosave timer, debug policy, error messages, and final shutdown-save placement. Settings passed 65+1/66, lifecycle families 27/27, Application 166+1/167, Contracts 73/73, and both Release targets 0W/0E. |
| SETCLOSE-001 | Done | agent-ar | independent verification and campaign close | Settings projection and persistence coordination | This row's containing closure commit starts from integrated `8f68c09c0a89bcd0424e88c97d054ba7a390ff97`; the returned handoff SHA is the non-self-referential commit identifier. AR reproduced coordinator 7/7, settings 65+1/66, lifecycle 27/27, Application 166+1/167, Contracts 73/73, aggregate 307+1/308, both WinForms Release targets 0W/0E, Avalonia 75/75 with established AVLN3001, web 315/315 plus 18/18, and final CI 8/8 included. AP/AQ ancestry, patches, blobs, protected diff, golden blob/SHA, affirmative plan validation, high-confidence analyzer, docs/truth agreement, sanctioned render idempotence, Git checks, and guarded cleanup pass. Fresh read-only live proof at 2026-08-02T03:19:19+01:00 retained PID 11980, exact Running task/listener, three HTTP 200s, and CSV growth of 12,885 bytes in six seconds. Plan status remains `executed`, ledger `implemented` with all 11 criteria met, Plan-012/agent `as` is next, A1 remains person-only, and no candidate or live change occurred. |
