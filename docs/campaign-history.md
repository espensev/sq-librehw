# Campaign History

**Status:** active
**Authority:** human acceptance evidence

This is the durable, tracked record of what a person has actually accepted.
`data/tasks.json` is local execution state, it is ignored by Git, and it can
never substitute for a row in this file. A row here records acceptance of a
campaign's own exit criteria. It is not authority to deploy, promote, or
change any live runtime.

Plan lifecycle status and ledger state answer different questions. Plan status
answers "has the tooling registered and run this campaign"; `executed` means the
agents were registered and their templates generated, not that the work is
finished. Ledger state answers "has a person accepted the result". The two move
independently and neither implies the other. The full contract, including who
may set each state and the required waiver fields, is in
`docs/architecture/campaign-control-plane.md`.

Criterion text in this file is copied verbatim from each plan's
`plan_elements.exit_criteria`. `scripts/task_runtime/test_campaign_history.py`
fails if the two ever disagree.

## Ledger

| Plan | Campaign | Plan status | Ledger state | Met | Open | Waived | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| plan-001 | Fixture-only Avalonia sensor explorer | partial | implemented | 8 | 1 | 0 | `docs/campaign-plan-001-fixture-only-avalonia-sensor.md` |
| plan-002 | Non-deploying CI gates and campaign history contract | executed | accepted | 13 | 0 | 0 | `docs/campaign-plan-002-non-deploying-ci-gates.md` |
| plan-003 | Move the Avalonia Fixture Explorer into experiments/ | executed | implemented | 9 | 0 | 0 | `docs/campaign-plan-003-move-the-avalonia-fixture.md` |
| plan-004 | Separate candidate creation from peer deployment | approved | implemented | 10 | 0 | 0 | `docs/campaign-plan-004-separate-candidate-creation-from.md` |
| plan-005 | Documentation and engineering taxonomy | executed | implemented | 10 | 0 | 0 | `docs/campaign-plan-005-documentation-and-engineering-taxonomy.md` |
| plan-006 | Verification suite boundaries | executed | implemented | 10 | 0 | 0 | `docs/campaign-plan-006-verification-suite-boundaries.md` |
| plan-007 | Characterization tests before extraction | executed | implemented | 10 | 0 | 0 | `docs/campaign-plan-007-characterization-tests-before-extraction.md` |
| plan-008 | Immutable sensor snapshot and data.json projection | executed | implemented | 10 | 0 | 0 | `docs/campaign-plan-008-immutable-sensor-snapshot-and.md` |

## plan-001 — Fixture-only Avalonia sensor explorer

Its automated execution manifest is `verified`, its tracked plan contract is
`partial`, and its ledger state is `implemented`. All three are simultaneously
correct: the automated work finished, an attended criterion is still open, and
no person has accepted the campaign.

**Open question for the owner.** Criterion 1 requires verification *before* the
implementation agents launch. AVSPIKE-001 records that the independent
dual-shell current-source check happened afterwards, not before. It is recorded
`met` with the deviation stated, matching the position the plan's own backfill
reason already took. Moving it to `open`, or adding a waiver record, is a
one-line edit here and the regression will enforce whichever is chosen.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | The accepted spec and plan checkpoint is committed and independently verified as a clean promotable current-source candidate before implementation agents launch. | met | Spike spec and plan checkpoint committed. Candidate `0.9.6-20260730-210528120-b466837` passed dual-shell exact-source promotable verification. Ordering deviation: `live-tracker.md` AVSPIKE-001 records that the independent dual-shell current-source verification was not recorded *before* the implementation agents launched, only afterwards. The substance was verified; the timing clause was not. |
| 2 | The separate net10.0 spike solution restores and builds with Avalonia 12.1.0 while LibreHardwareMonitor.sln, global.json, and existing project files remain unchanged. | met | The separate `net10.0` spike solution restores and builds with Avalonia 12.1.0 while `LibreHardwareMonitor.sln`, `global.json`, and the existing project files remain unchanged. AVSPIKE-001. |
| 3 | The parser enforces the 4 MiB, depth 32, 16384-node, 4096-child, 1024-character, unique-ID, single-snapshot, and single-load bounds with no partial state. | met | 49 of 49 parser and replacement cases enforce every byte, depth, node, child, string, and identity bound with atomic immutable projection. AVSPIKE-002. |
| 4 | Recorded and generated tests cover normal, unavailable, hot-plug removal, malformed, oversized, duplicate-ID, and every hard-limit case. | met | Recorded and generated fixtures cover the normal, unavailable, hot-plug removal, malformed, oversized, duplicate-ID, and hard-limit cases. Residual: a reliable Windows ACL-denied test remains unavailable, although typed catches exist, and a non-cooperative caller stream can outlive logical supersession. AVSPIKE-002. |
| 5 | The Avalonia shell renders producer order, stable IDs, honest current/min/max values, and loaded, empty, loading, and rejection states with keyboard navigation. | met | 19 of 19 UI and headless cases cover explicit states, accessible labels, keyboard focus and navigation, cancellation, and safe errors. Residual: the `AVLN3001` source-build warning remains. AVSPIKE-003. |
| 6 | No spike project references WinForms, opens hardware, requests elevation, writes current settings, exposes control requests, joins packaging, or changes live state. | met | Both WinForms package inventories contained zero Avalonia or spike entries. The spike is absent from `LibreHardwareMonitor.sln`, from every scheduled task, and from the live runtime. AVSPIKE-004. |
| 7 | Existing DataJsonGoldenTests, the complete .NET test suite, and both x64 Release WinForms targets pass unchanged. | met | The .NET suite passed 258 with one documented opt-in skip, and both x64 Release WinForms targets, `net10.0-windows` and `net472`, passed. Re-proved by the plan-002 gate sweep on 2026-07-31. |
| 8 | The release-system fixture passes and candidate inspection proves spike outputs are absent from WinForms release packages. | met | The release-system fixture passed 114 of 114 assertions and candidate inspection proved spike outputs absent from the WinForms release packages. AVSPIKE-004. |
| 9 | An attended normal-user smoke and documentation evidence record the feasibility result and any separately gated polling recommendation. | open | The attended normal-user smoke and its documentation evidence record have not been performed. This is the only reason plan-001 remains `partial`. Closing it requires either performing the attended smoke or filing a waiver record in this file. |

**Maintainer deferral (2026-07-31):** The attended normal-user smoke (criterion 9) remains `open` by maintainer decision so that plan-003 — the source-only relocation of this explorer into `experiments/avalonia-fixture-explorer/` — may proceed. It is not waived and not met. The relocation changes only the explorer's source location, not its behavior, so the smoke can be performed against the moved build afterwards.

## plan-002 — Non-deploying CI gates and campaign history contract

Every criterion has recorded evidence and all agent work is in the working
tree. The campaign that wrote the acceptance contract could not record its own
acceptance, so it remained `implemented` until a person decided otherwise.

**Accepted by the maintainer on 2026-08-01** (standing action A2, authorized in
the maintainer's session). All 13 criteria were `met` with recorded evidence,
satisfying the transition rule this campaign itself defined; automation
recorded the transition only after the explicit person decision.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | eng/ci/Invoke-LhmGates.ps1 -List prints every [build-gate.*] name defined in .codex/skills/project.toml with its included or excluded classification and reason, and exits 0. | met | `-List` exits 0 and prints all nine configured gates with classification and reason. `Test-GateRunner.ps1` cases 1 and 2 assert the listed set against the configuration parsed by the same `tomllib` call the runner uses, so the assertion cannot go stale when a gate is added. |
| 2 | Every build gate name in .codex/skills/project.toml is explicitly classified by the runner; an unclassified gate name makes the runner exit non-zero with an unclassified-gate error instead of silently skipping it. | met | `Test-GateRunner.ps1` case 3: a fixture declaring `[build-gate.rogue]` exits 2 and names the gate. The case asserts the specific unclassified-gate message rather than the bare exit code, because a configuration parse failure also exits 2. |
| 3 | The runner refuses, without executing, any command matching the deploying or host-mutating deny-list, and the release-candidate build and -RequireCurrentSource verify keys are excluded from the runnable set with a recorded reason while its release-system fixture test remains included. | met | `Test-GateRunner.ps1` case 4: an included gate carrying `New-LhmRelease.ps1` exits 3 and the sentinel that command would have written does not exist, proving refusal before execution. Case 5: `release-candidate` resolves only `Test-LhmReleaseSystem.ps1`, and neither `New-LhmRelease` nor `-RequireCurrentSource` appears. |
| 4 | eng/ci/Invoke-LhmGates.ps1 -All runs the included gate set on identity-verified SND-HOST, reports a per-gate result table, and exits non-zero if any gate fails. | met | Full `-All` sweep run twice on identity-verified `snd-host`: 8 of 8 included gates passed, per-gate result table emitted, `ci-gates` correctly skipped. `Test-GateRunner.ps1` case 10 proves a failing gate exits 1. |
| 5 | powershell.exe -NoProfile -ExecutionPolicy Bypass -File eng\ci\Test-LhmCiGates.ps1 discovers and runs every eng/ci/tests/Test-*.ps1, reports an aggregate pass and fail count, and exits non-zero on any failure. | met | `Test-LhmCiGates.ps1` discovers both test scripts and reports 22 passed, 0 failed, 0 skipped under Windows PowerShell 5.1 and PowerShell 7. An empty or missing test root exits 0, so the `ci-gates` gate cannot fail on an unpopulated suite. |
| 6 | .github/workflows/non-deploying-gates.yml parses as valid YAML, declares permissions contents read, references no repository secret and no deployment or publish action, and every gate-running step delegates to eng/ci/Invoke-LhmGates.ps1. | met | `Test-Workflow.ps1`: 11 assertions pass, including YAML parse, `permissions: contents: read` and nothing else, no `secrets.` reference, no deployment or cloud-login action, and every `run` step delegating to the runner. The three safety-critical assertions were negative-tested — broadened permissions, an inline `dotnet test`, and an injected secret each failed the suite — after which the workflow was restored byte-identical. |
| 7 | docs/campaign-history.md exists as a tracked ledger with one row per plan file in data/plans/, uses the ledger states registered, implemented, accepted, and closed, and records plan-001 with its attended exit criterion still open. | met | This file. One row per plan file in `data/plans/`, the four ledger states, and plan-001 recorded with criterion 9 open. |
| 8 | python -m unittest discover -s scripts -p test_*.py passes, including a new campaign-history regression that fails if any campaign is recorded accepted or closed while one of its exit criteria is still open or is waived without a complete waiver record, and that fails if the ledger and data/plans/ disagree on the criterion count. | met | `python -m unittest discover -s scripts -p test_*.py` passes 31 of 31, up from 12. Nineteen are new; eleven of those exercise the rule functions against deliberately broken fixtures, so the rules are proven to reject rather than merely to pass. |
| 9 | docs/architecture/campaign-control-plane.md states the campaign-history transition contract including the waiver record fields, and lists the added repository-owned overlay test with its normalization-aware Git blob ID. | met | `docs/architecture/campaign-control-plane.md` gained the acceptance ledger as a fourth authority layer, the two-axis explanation, the four ledger states with named owners, the transition rule, and the five mandatory waiver fields. `test_campaign_history.py` is listed as the fourth repository-owned overlay test with blob ID `58a37e2c879dd2fce554c5c6ac4efc978572b08e`. |
| 10 | .codex/skills/project.toml declares the ci module, the ci-gates build gate, the eng/ci and .github/workflows smart-test mappings, and the gate-definition-drift conflict zone, and python scripts/task_manager.py plan preflight --json reports ready with zero errors. | met | `.codex/skills/project.toml` declares `ci`, `[build-gate.ci-gates]`, the `eng/ci`, `eng/ci/tests`, `.github/workflows`, and `docs/campaign-history.md` smart-test mappings, and both new conflict zones. `plan preflight --json` reports ready with zero errors. |
| 11 | Both WinForms x64 Release targets and the .NET test suite still pass, and git diff --stat shows no change under Aga.Controls, LibreHardwareMonitorLib, LibreHardwareMonitor.Windows.Forms, LibreHardwareMonitor.Tests, or LibreHardwareMonitor.sln. | met | Both WinForms x64 Release targets and the .NET suite passed in the final sweep, and `git diff --stat` over `Aga.Controls`, `LibreHardwareMonitorLib`, `LibreHardwareMonitor.Windows.Forms`, `LibreHardwareMonitor.Tests`, `LibreHardwareMonitor.sln`, `Directory.Packages.props`, and `global.json` is empty. |
| 12 | data/plans/plan-001.json still has status partial with an empty executed_at, and no scheduled task, live runtime file, release candidate, rollback packet, or log archive changed during the campaign. | met | `data/plans/plan-001.json` is still `partial` with an empty `executed_at`, asserted by `test_campaign_history.py`. Live proof after the sweep: one process at PID 13104 on the live executable, root task `Running`, CSV grown to 78.9 MB, and `/`, `/data.json`, `/metrics` all HTTP 200. No release candidate, rollback packet, or log archive was created or changed. |
| 13 | docs/HANDOFF.md is retired only after docs/refactor-roadmap.md and docs/README.md carry its non-negotiable architecture decisions, do-not list, live-verification method, and remaining-ambiguity list. | met | All thirteen carry-over rows were verified present in `docs/refactor-roadmap.md` or `docs/README.md` before deletion. `AGENTS.md` now names `docs/refactor-roadmap.md` as the continuation checkpoint and additionally links `docs/campaign-history.md` and `eng/ci/README.md`. `docs/HANDOFF.md` was removed with `git rm`; the original 219-line file remains recoverable at `HEAD`. Surviving mentions are in completed agent specs, which are immutable campaign evidence and are deliberately not rewritten. |

## plan-003 - Move the Avalonia Fixture Explorer into experiments/

Source-only relocation of the fixture-only, non-shipping Avalonia explorer under `experiments/avalonia-fixture-explorer/`, preserving project/assembly names, behavior, history, and every WinForms/live boundary. Ledger state is `registered` while the campaign gates run; it moves to `implemented` once every criterion has recorded evidence. Not `accepted`: that transition is person-only.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | The three projects, the .slnx, and the test runner live under experiments/avalonia-fixture-explorer/, moved with git mv so git log --follow resolves through the move. | met | Moved with `git mv`: the three projects, the `.slnx`, and the relocated `Test-AvaloniaSpike.ps1` are under `experiments/avalonia-fixture-explorer/`; 44 renames detected by git as renames so history follows. |
| 2 | dotnet build of the moved .slnx succeeds in Release from a clean output state. | met | `dotnet build` of the moved `.slnx` in Release succeeded with 0 errors (1 pre-existing AVLN3001 warning, unrelated to the move) from a clean output state. |
| 3 | The Avalonia gate passes 75/75, unchanged in count. | met | The relocated `avalonia-spike` gate (`Test-AvaloniaSpike.ps1`) passed unchanged at 75/75 in the `Invoke-LhmGates -All` sweep. |
| 4 | eng/ci/Invoke-LhmGates.ps1 -All passes 8/8 with no edit to the runner, proving configuration-only rewiring. | met | `Invoke-LhmGates.ps1 -All` passed 8/8 on 2026-07-31 from the primary checkout at `9fc9bd2` with the runner byte-identical: all eight gates exit 0, including `snd-desk-local-release-fixture` (21.6s, `WindowsPowerShellLauncherCompatibility` and `WindowsPowerShellCleanupCompatibility` both True). Root cause of the earlier 7/8: the launching shell's process `PSModulePath` carried PowerShell 7 module directories, which Windows PowerShell 5.1 resolves `Microsoft.PowerShell.Utility` from, hiding `Get-FileHash`. The persisted user and machine `PSModulePath` values are the clean defaults, so no machine or runner change was required; re-running the sweep with a sanitized process `PSModulePath` (the two WindowsPowerShell module directories) made the fixture pass under `powershell.exe`. Not a plan-003 regression, now proven green end to end. |
| 5 | Both WinForms x64 Release targets and the .NET suite pass, and git diff --stat shows no change under inherited product roots. | met | Both WinForms x64 Release targets (`net10.0-windows`, `net472`) and the .NET suite passed in the `-All` sweep, and `git diff --stat` over `Aga.Controls`, `LibreHardwareMonitorLib`, `LibreHardwareMonitor.Windows.Forms`, `LibreHardwareMonitor.Tests`, `LibreHardwareMonitor.sln`, `Directory.Packages.props`, and `global.json` is empty. |
| 6 | The permanent stale-reference test passes and the CI dispatcher reports three discovered test scripts. | met | `eng/ci/tests/Test-NoStaleReferences.ps1` passes (exit 0), and the CI dispatcher `Test-LhmCiGates.ps1` discovers three test scripts. |
| 7 | A clean candidate built from the moved source contains zero Avalonia or spike entries in both WinForms package inventories. It is not promoted. | met | Candidate `0.9.6-20260731-123549167-79928cb` built from the committed moved source (commit 79928cb), promotable and current-source (source fingerprint `395e8bfa7ade849fb916f3fdf74532c80c0523b16f1211ae40e62831f7d0ba37`). Both WinForms package ZIPs (net10.0-windows, net472) and the release manifest contain zero Avalonia/spike files and zero Avalonia/spike strings; `Test-LhmReleaseCandidate -Latest -RequirePromotable -RequireCurrentSource` passed (TESTEXIT=0). Not promoted. |
| 8 | No product source, live runtime, scheduled task, release store, rollback packet, active log, or archive changed. | met | Live SND-HOST proof after the campaign: one process (PID 13104) at the live executable, root task `\LibreHardwareMonitor` `Running` (result 267009) with action and working directory matching the live root, proxy-bypassed `/`, `/data.json`, and `/metrics` all HTTP 200, current-day CSV at 110 MB and still growing. No live runtime, task, config, release store, rollback packet, or log archive changed. |
| 9 | plan preflight --json is ready with zero errors and, after guarded cleanup, git clean -ndX lists only data/tasks.json and data/analysis-cache.json. | met | `plan preflight --json` reports ready with zero errors, and after guarded cleanup via `Clear-LhmRepositoryBuildOutputs.ps1`, `git clean -ndX` lists only `data/tasks.json` and `data/analysis-cache.json`. |

## plan-004 - Separate candidate creation from peer deployment

Source-only relocation splitting host-neutral candidate creation (`ops/candidate`), SND-DESK-only peer deployment (`ops/deploy/snd-desk`), and repository maintenance (`eng/`) so the path states the blast radius. The tracked plan lifecycle remains `approved` with an empty `executed_at`, while the independently recorded ledger state is `implemented` with all ten criteria met. Not `accepted`: that transition is person-only. Note: the `snd-desk-local-release-fixture` gate was blocked during this campaign by the then-pre-existing Windows PowerShell 5.1 `Get-FileHash` defect (shared with Plan-003 criterion 4); the fail-closed proof ran under `pwsh`.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | ops/release/ is relocated to ops/candidate/, ops/local-release/ plus the deploy scripts to ops/deploy/snd-desk/, Clear-LhmRepositoryBuildOutputs.ps1 to eng/, ops/log-management/ is unchanged, and scripts/local-release/ is dissolved - all via git mv so git log --follow resolves through the moves. | met | MET: ops/release/ relocated to ops/candidate/ (4 files), ops/local-release/ (7 files) plus scripts/local-release/{Publish,Test} to ops/deploy/snd-desk/ (9 files), Clear-LhmRepositoryBuildOutputs.ps1 to eng/, ops/log-management/ unchanged, scripts/local-release/ dissolved. All via git mv; 14 renames detected by git so history follows. |
| 2 | Every internal PSScriptRoot and repository-root reference in the moved scripts resolves after the move: dot-sourcing of LhmLocalRelease.Common.ps1 and LhmRelease.Common.ps1 works, and the relocated Test-LhmLocalRelease.ps1 cleanup reference points at eng/Clear-LhmRepositoryBuildOutputs.ps1. | met | MET: every internal reference resolves after the move. Test-LhmLocalRelease.ps1 repo-root depth ($PSScriptRoot '..\..\..'), $opsRoot=$PSScriptRoot (co-located), cleanup ref to eng\; Publish repo-root depth + Common ref; Clear repo-root depth. Dot-sourcing verified by the peer-safe fixture passing (exit 0, all guards True). |
| 3 | dotnet build of the shipping solution, both WinForms x64 Release targets, and the .NET suite pass, and git diff --stat shows no change under inherited product roots. | met | MET: Invoke-LhmGates.ps1 -All: winforms-net10 Passed, winforms-net472 Passed, .NET suite Passed (258/259, 1 skipped). git diff --stat over inherited product roots (Aga.Controls, LibreHardwareMonitorLib, LibreHardwareMonitor.Windows.Forms, LibreHardwareMonitor.Tests, LibreHardwareMonitor.sln, Directory.Packages.props, global.json) is empty. |
| 4 | .codex/skills/project.toml modules, smart-test mappings, the release-candidate and snd-desk-local-release-fixture build gates, and conflict zones point at the new paths; plan preflight --json is ready with zero errors; and eng/ci/Invoke-LhmGates.ps1 is byte-identical. | met | MET: project.toml modules (candidate-ops, snd-desk-deploy-ops), smart-test mappings, release-candidate + snd-desk-local-release-fixture build gates, and conflict zones point at new paths; plan preflight --json ready with zero errors; eng/ci/Invoke-LhmGates.ps1 byte-identical (git diff --stat empty). |
| 5 | The permanent stale-reference gate (Test-NoStaleReferences.ps1) is extended to cover the ops/release, ops/local-release, and scripts/local-release moves, and passes; no tracked current configuration or current document references a dissolved path. | met | MET: Test-NoStaleReferences.ps1 extended with ops/release->ops/candidate, ops/local-release->ops/deploy/snd-desk, scripts/local-release/Clear->eng move-map entries and a dissolved-path content check. Gate passes (exit 0); no tracked current configuration or document references a dissolved path. |
| 6 | The content-based deny-list still refuses every deploying command after the move, proven by Invoke-LhmGates.ps1 -DryRun/-List and the gate-runner regression. | met | MET: the deny-list is content-based (New-LhmRelease, Publish-LibreHardwareMonitor, Install-, Finalize-, Restore-Legacy, Clear-LhmRepositoryBuildOutputs, etc.) and survives the move unchanged. -List/-DryRun confirm deploying commands are excluded/refused; the gate-runner regression (Test-LhmCiGates.ps1) passes 35/35 assertions. |
| 7 | Every SND-DESK fail-closed guard still fails closed on SND-HOST after the move, proven by the relocated peer-safe fixture (run under pwsh if the Windows PowerShell 5.1 Get-FileHash defect persists). | met | MET: the relocated peer-safe fixture (ops/deploy/snd-desk/Test-LhmLocalRelease.ps1) passes under pwsh (EXIT=0): 12 failure-injection cases, 16 hostile-recovery cases, 12 hostile-reparse cases, and all compatibility/cleanup/shim/log guards True. The configured gate runs under powershell.exe where Get-FileHash is unavailable (pre-existing PS 5.1 PSModulePath defect, shared with Plan-003 criterion 4); the fail-closed property is proven via pwsh. |
| 8 | The Avalonia 75-test gate still passes after its ops\release\Test-LhmReleaseSystem.ps1 reference is rewired to ops\candidate. | met | MET: the avalonia-spike gate passed 75/75 in the -All sweep after its ops\release\Test-LhmReleaseSystem.ps1 reference was rewired to ops\candidate\Test-LhmReleaseSystem.ps1. |
| 9 | No product source, live runtime, scheduled task (including the installed ops/log-management SYSTEM task), release store, rollback packet, active log, or archive changed, and LHM_RELEASE_ROOT is untouched. | met | MET: live SND-HOST proof after the campaign: one process (PID 13104) at the live executable, root task \LibreHardwareMonitor Running (result 267009) with action and working directory matching the live root, proxy-bypassed /, /data.json, and /metrics all HTTP 200, current-day CSV at 124 MB and growing. No product source, live runtime, scheduled task (including the installed ops/log-management SYSTEM task), release store, rollback packet, active log, or archive changed. LHM_RELEASE_ROOT untouched. |
| 10 | The Plan-004 ledger row is added with criterion-specific evidence and is not marked accepted; after guarded cleanup, git clean -ndX lists only data/tasks.json and data/analysis-cache.json. | met | MET: the Plan-004 ledger row is added with criterion-specific evidence and is not marked accepted (state implemented, person-only to accept). After guarded cleanup via eng/Clear-LhmRepositoryBuildOutputs.ps1, git clean -ndX lists only data/tasks.json and data/analysis-cache.json. |

## plan-005 - Documentation and engineering taxonomy

Group hand-authored content docs under docs/features/ and docs/architecture/, retire completed point-in-time discovery reviews, and update every reference, while leaving tooling-coupled docs at docs/ root and scripts/task_manager.py unmodified. Ledger state is `registered` while the campaign gates run. Not `accepted`: person-only.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | The 11 feature-*.md live under docs/features/, refactor-roadmap.md and repository-build-output-cleanup.md under docs/architecture/, and the 2 discovery-*.md are retired with a fold note - all via git mv / git rm so history is preserved. | met | MET: 13 feature-*.md under docs/features/, refactor-roadmap.md and repository-build-output-cleanup.md under docs/architecture/, 2 discovery-*.md retired (git rm; findings folded into roadmap). All via git mv/git rm; git log --follow resolves. |
| 2 | Tooling-coupled docs stay at docs/ root: docs/README.md, campaign-backlog.md, campaign-history.md, campaign-playbook.md, and the 4 rendered campaign-plan-*.md are unmoved. | met | MET: Tooling-coupled docs unmoved: docs/README.md, campaign-backlog.md, campaign-history.md, campaign-playbook.md, and 4 rendered campaign-plan-*.md all at docs/ root. |
| 3 | scripts/task_manager.py is unmodified (git diff --stat empty) and test_campaign_history.py LEDGER_PATH still resolves; plan_doc_path still renders to docs/campaign-plan-*.md. | met | MET: git diff --stat over scripts/task_manager.py is empty (canonical-surface, provenance-pinned, unmodified); _plan_doc_path still renders to docs/campaign-plan-*.md; test_campaign_history.py LEDGER_PATH resolves (regression 19/19). |
| 4 | Every current reference to a moved doc path is updated: AGENTS.md source map, docs/README, campaign-playbook, campaign-backlog, and inter-doc links. | met | MET: Every current reference updated - 45 path replacements across AGENTS.md, docs/README, campaign-playbook, campaign-backlog, inter-doc links, and the 2 deploy scripts. Stale-reference gate passes (exit 0). |
| 5 | .codex/skills/project.toml module globs, smart-test mappings, and conflict-zone paths agree with the new layout; plan preflight --json is ready with zero errors. | met | MET: project.toml module glob docs/ covers nested; docs-sync.tier2 globs docs/**/*.md; plan preflight --json ready with zero errors. |
| 6 | The permanent stale-reference gate is extended to cover the doc moves (move-map entries + dissolved discovery-path check) and passes; no tracked current configuration or document references a retired path. | met | MET: Test-NoStaleReferences.ps1 extended with docs/refactor-roadmap and docs/repository-build-output-cleanup move-map entries plus a retired-discovery/bare-doc-path content check. Gate passes (exit 0). |
| 7 | Both WinForms x64 Release targets and the .NET suite pass, and git diff --stat shows no change under inherited product roots. | met | MET: Invoke-LhmGates.ps1 -All: winforms-net10 Passed, winforms-net472 Passed, .NET suite Passed. git diff --stat over inherited product roots is empty. |
| 8 | eng/ci/Invoke-LhmGates.ps1 is byte-identical and the full gate sweep passes (modulo the pre-existing snd-desk-local-release-fixture Get-FileHash defect). | met | MET: Invoke-LhmGates.ps1 byte-identical; -All passed 7/8 (snd-desk-local-release-fixture fails on the pre-existing PS 5.1 Get-FileHash defect shared with Plan-003/004; not a Plan-005 regression). |
| 9 | No product source, live runtime, scheduled task, release store, rollback packet, active log, or archive changed. | met | MET: live SND-HOST proof: one process (PID 13104) at the live executable, root task Running (267009), /, /data.json, /metrics all HTTP 200, current-day CSV 130 MB and growing. No live runtime, task, config, release store, rollback packet, or log/archive changed. |
| 10 | The Plan-005 ledger row is added with criterion-specific evidence and is not marked accepted; after guarded cleanup, git clean -ndX lists only data/tasks.json and data/analysis-cache.json. | met | MET: Plan-005 ledger row added with criterion-specific evidence, not marked accepted (state implemented). After guarded cleanup, git clean -ndX lists only data/tasks.json and data/analysis-cache.json. |

## plan-006 - Verification suite boundaries

Split of the flat `LibreHardwareMonitor.Tests` project into four boundary-aligned suites - Library, Application, Contracts, and the attended-outside-CI Attended suite - behind the deterministic `LibreHardwareMonitor.Tests.slnf`, preserving every test case, the golden-master bytes, the single opt-in skip, and both WinForms x64 Release builds. Ledger state is `implemented` with all ten criteria met, including the maintainer's post-close full gate sweep; not `accepted`: that transition is person-only.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | The four suite projects (Library, Application, Contracts, Attended) and the LibreHardwareMonitor.Tests.slnf solution filter live under LibreHardwareMonitor.Tests/; all 27 test source files and data.golden.json are relocated with git mv so git log --follow resolves; the old LibreHardwareMonitor.Tests.csproj is removed. | met | The four suite projects and the slnf live under `LibreHardwareMonitor.Tests/`; git records 28 R100 renames for the 27 test sources plus `data.golden.json`, and the old `LibreHardwareMonitor.Tests.csproj` shows as D (deleted). Independently re-verified by agent aa: 28/28 renames R100, old csproj D. |
| 2 | Boundary integrity holds: the Library suite references LibreHardwareMonitorLib only (no WinForms reference in its project or built dependency set); the Contracts suite holds the data.json golden, HTTP route/authentication/lifetime, Prometheus, CSV timestamp, and web-dashboard-retirement tests; the Application suite holds the 13 WinForms behavior test files including the preserved LHM_LIVE_CONFIG_PATH opt-in skip. | met | The Library csproj references only `LibreHardwareMonitorLib` (no UseWindowsForms, no WPF) and its rebuilt bin contains no `LibreHardwareMonitor.Windows.Forms.dll` or `Aga.Controls.dll` (the Application bin does, as positive control). The Contracts suite holds the data.json golden, HTTP authentication/lifetime/Prometheus/sensor-API, CSV timestamp, and web-dashboard-retirement tests; the Application suite holds the 13 WinForms behavior test files including the preserved `LHM_LIVE_CONFIG_PATH` opt-in skip. |
| 3 | data.golden.json is byte-identical - git records a pure rename - and DataJsonGoldenTests passes against it without regeneration. | met | Pure R100 rename with zero content hunks; `git hash-object` of the moved golden equals the HEAD blob `05113704acc6fefeb4128004b3f523d876fbcec4`, 2015 bytes both sides. The filtered DataJsonGoldenTests run passed 2/2 against the moved, unregenerated golden. |
| 4 | The three deterministic suites together discover 259 test cases with 258 passed and exactly the one pre-existing opt-in skip; no test case is lost, duplicated, or newly skipped relative to the recorded pre-campaign baseline. | met | Per-suite totals Library 64/64/0, Application 131/130/1 (skip = `SettingsPersistenceTests.LiveConfigCopy_LoadsAndCompactsWithinMemoryBudgets`, the `LHM_LIVE_CONFIG_PATH` gate), Contracts 64/64/0 sum to 259 discovered / 258 passed / 1 skipped, matching the recorded `cae786c` baseline exactly; the slnf run reproduced the same totals over exactly the three deterministic assemblies. |
| 5 | The Attended suite exists in LibreHardwareMonitor.sln, is absent from the slnf and from every configured gate command, and the new permanent eng/ci/tests/Test-SuiteBoundaries.ps1 proves that exclusion and passes; the CI dispatcher discovers four test scripts. | met | The Attended suite is in `LibreHardwareMonitor.sln` (GUID {061167BD-82C2-4E32-B6D9-F9621D2AA71B}), absent from the slnf and from every `[build-gate.*]` command; the permanent `Test-SuiteBoundaries.ps1` proves the exclusion and passes 13/13; the CI dispatcher discovers four test scripts under both pwsh 7 and Windows PowerShell 5.1. |
| 6 | git diff over the inherited product roots is limited to the InternalsVisibleTo ItemGroups of LibreHardwareMonitorLib.csproj and LibreHardwareMonitor.Windows.Forms.csproj plus the LibreHardwareMonitor.sln test-project graph; both WinForms x64 Release targets build. | met | The product-root diff is limited to the two csproj InternalsVisibleTo ItemGroups (one entry to four suite names each) plus the `LibreHardwareMonitor.sln` 39+/9- test-project-graph swap, with `Aga.Controls/`, `Directory.Packages.props`, and `global.json` untouched. Both WinForms x64 Release targets (`net10.0-windows`, `net472`) built with 0 warnings, 0 errors. |
| 7 | .codex/skills/project.toml commands, modules, smart-test mappings, conflict zones, and the winforms-net10 test command point at the new layout; the ops/candidate, ops/deploy/snd-desk, Test-AvaloniaSpike, and eng/Clear-LhmRepositoryBuildOutputs references are rewired; plan preflight --json is ready with zero errors; eng/ci/Invoke-LhmGates.ps1 is byte-identical. | met | `project.toml` `test`/`test_fast`, the `dotnet-tests` module (renamed from `winforms-tests`), the data.json conflict zone, the smart-test mappings, and the `winforms-net10` test command all point at the slnf/suite layout; `New-LhmRelease.ps1` and `LhmRelease.Common.ps1` run the slnf, `Publish-LibreHardwareMonitor.ps1` loops the three deterministic suites with per-suite OutputPath, `Test-AvaloniaSpike.ps1` runs the slnf and sums per-assembly totals, and `eng/Clear-LhmRepositoryBuildOutputs.ps1` lists the eight per-suite `bin`/`obj` paths. `plan preflight --json` is ready with zero errors; `eng/ci/Invoke-LhmGates.ps1` is byte-identical (empty diff). |
| 8 | The extended stale-reference gate covers all 28 file relocations and the dissolved csproj path, including a purpose-built check for file types outside its default scan set, and passes; eng/ci/Invoke-LhmGates.ps1 -All passes from the campaign tree. | met | The extended gate carries all 28 per-file move-map entries plus dissolved-csproj and new-artifact checks with a purpose-built textual block widened to `.csproj`/`.sln`/`.slnf`/`.json`, and after the Plan-006 docs pass it exits 0 with the CI dispatcher green across all four discovered scripts (86/0/0). Maintainer close sweep 2026-08-01 from the final tree with sanitized process `PSModulePath`: `Invoke-LhmGates.ps1 -All` passed 8/8 included gates, 0 failed, 0 refused, `ci-gates` skipped as excluded, exit 0 — including campaign-control (preflight ready; unittest 31/31 with this ledger section present), the slnf-based winforms-net10 test, the 114-assertion release fixture, and `snd-desk-local-release-fixture` under `powershell.exe`. Agent aa's earlier 7/8 sweep is superseded: its sole red was campaign-control's unittest failing 2/31 only because this ledger row did not yet exist. |
| 9 | No product source behavior, live runtime, scheduled task, release store, rollback packet, active log, or archive changed - proven by the standard five-point live SND-HOST check. | met | Five-point live SND-HOST proof unchanged except CSV growth: one process (PID 32244) at the live executable, root task `\LibreHardwareMonitor` Running (result 267009) with matching action and working directory, proxy-bypassed `/`, `/data.json`, and `/metrics` all HTTP 200 on the bound LAN address (localhost refused; socket owned by PID 4), and the 2026-08-01 CSV grew +254,931 bytes over ~109 s. No release store, rollback packet, scheduled task, or archive changed. |
| 10 | The Plan-006 ledger row is added with criterion-specific evidence and is not marked accepted; the Phase 3 suite-split roadmap items are closed with characterization tests left open for plan-007; the backlog advances; after guarded cleanup, git clean -ndX lists only data/tasks.json and data/analysis-cache.json. | met | This row and table, state `implemented`, not `accepted` (person-only). The three Phase 3 suite-split roadmap items are closed with (Plan-006, 2026-08-01) tags and the characterization item stays open for plan-007; the backlog's plan-006 section is removed and the current position advances to plan-007 with next agent letter `ac`. After guarded cleanup via `eng/Clear-LhmRepositoryBuildOutputs.ps1` (`-WhatIf` reviewed first), `git clean -ndX` lists only `data/tasks.json` and `data/analysis-cache.json`. |

## plan-007 - Characterization tests before extraction

Twenty deterministic, test-only facts now pin the current hardware-group lifetime, ordered option/reset, shutdown coordination, and settings projection/persistence behavior required before Phase-4 and Phase-5 extraction. The tracked plan lifecycle remains `executed`; the ledger state is `implemented` with all ten criteria met. It is not `accepted`: that transition remains person-only. No production or live-runtime state changed.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | The campaign diff outside campaign documentation is limited to the six exclusively owned test files: one new Library characterization file, one new Application projection file, and four existing Application test files; no production, project, package, gate, golden-master, or operational file changes. | met | `git diff --name-status e506fa6..ca5b471` lists exactly six test paths: new `HardwareGroupLifetimeCharacterizationTests.cs` and `SettingsProjectionTests.cs`, plus four modified Application test files. The diff is 1,064 insertions in those six files; all production, project, package, gate, golden-master, and operations paths are absent. This corrects the generated plan's seven-file arithmetic typo without changing implementation. |
| 2 | AC adds four passing hardware lifetime facts that prove Computer dynamic-group registration/forwarding/detachment/reverse close and rollback/retry, Nvidia partial factory cleanup and lease release, and Storage partial factory cleanup and unsubscribe behavior. | met | AC source `f019266` integrated as `691c6dc`. The independent `HardwareGroupLifetimeCharacterizationTests` filter passed 4/4 and the complete Library suite passed 68/68. The four fact names pin dynamic-group forwarding and reverse drain, rollback/retry, NVIDIA partial-factory cleanup plus lease release, and storage unsubscribe plus device close. |
| 3 | AD adds eight passing facts that prove active same-key option coalescing, reset follow-up ordering, typed failure events with continued drain, cancellation admission, UI-close takeover, BeginInvoke-style shutdown waiting, shared fault completion, and dispatch-failure retry. | met | AD source `f3ce3f7` integrated as `74592e4`. Its diff adds exactly four coordinator and four shutdown facts; the independent combined coordinator/shutdown filter passed 19/19, while the isolated AD Application suite was 139 discovered, 138 passed, and the one existing opt-in skip. |
| 4 | AE adds eight passing facts that prove UserOption and UserRadioGroup projection/event ordering and null-name behavior, malformed typed-value fallbacks, duplicate-key last-wins normalization, and managed-path startup fail-closed behavior. | met | AE source `628debb` integrated as `ca5b471`. Its diff adds exactly eight facts: six projection/typed-fallback cases, one duplicate-key normalization case, and one managed-install fail-closed case. The new-fact result was 8/8; AF's broader independent settings filter was 59 discovered, 58 passed, and the one existing opt-in skip. |
| 5 | All characterization tests use test-local fakes, in-memory settings, managed-path injection, or unique temporary directories; they do not construct MainForm or access live hardware, Task Scheduler, registry, listener ports, production configuration, or attended test surfaces. | met | Review of the six-file diff confirms AC uses test-local group/device factories, AD uses explicit in-memory barriers and dispatch delegates, and AE uses in-memory settings or unique temporary XML roots. No `MainForm`, live hardware, scheduler, registry, HTTP listener/socket, production-config path, or Attended reference is introduced; `listenerPort` appears only as a duplicate key in temporary XML normalization data. Both live-config variables were unset for every AF test run. |
| 6 | The deterministic solution filter discovers exactly 279 cases with 278 passed and exactly the pre-existing LHM_LIVE_CONFIG_PATH opt-in skip; every new case passes and no baseline case is lost, duplicated, or newly skipped. | met | Independent integrated run: Library 68/68, Contracts 64/64, and Application 147 discovered/146 passed/one skipped, totaling exactly 279 discovered, 278 passed, and one skipped. The only skip is the existing `SettingsPersistenceTests.LiveConfigCopy_LoadsAndCompactsWithinMemoryBudgets`; the population increased from 259/258/1 by exactly the 20 new passing facts. |
| 7 | The focused Library and Application filters, complete deterministic solution filter, WinForms x64 Release net10.0-windows and net472 builds, full CI gate sweep, plan preflight, and git diff checks all pass. | met | Restore and focused filters passed at 4/4, 19/19, and 58 passed plus one existing skip; the aggregate passed at 279/278/1. Direct Release x64 `net10.0-windows` and `net472` builds each passed with zero warnings and errors. After this ledger section existed, `Invoke-LhmGates.ps1 -All` passed all eight included gates with `ci-gates` skipped only because it is self-referential. Pre-commit planner preflight was ready with zero errors and only the expected dirty-worktree warning; the post-commit clean-tree preflight returned zero errors/warnings. Git diff checks passed. The initial pre-ledger sweep's sole red was the expected campaign-history regression for this then-missing row and criterion table. |
| 8 | Exclusive worktree ownership is preserved, each implementation lane returns a committed test-only change, integration is smallest-diff-first, and no unrelated dirty or ignored state is merged or removed. | met | Worktree inventory shows isolated AC `f019266`, AD `f3ce3f7`, AE `628debb`, and AF lanes. Main integrated smallest-diff-first as `691c6dc`, `74592e4`, then `ca5b471`; each implementation commit contains only its one, two, or three owned test files. AF owns only the six campaign-truth files. No ignored state was staged, merged, cleaned, or removed. |
| 9 | Read-only SND-HOST proof confirms the live deployment remains separate and healthy while scheduled tasks, settings, logs, release store, rollback store, and the non-Git operations tree remain untouched. | met | Identity verifier returned `VERIFIED` for `snd-host`. At 2026-08-01 12:50:40+01:00, exactly one process (PID 14876) ran the declared live executable; root task `\LibreHardwareMonitor` was `Running` with result 267009 and matching action/working directory; proxy-bypassed `/`, `/data.json`, and `/metrics` each returned HTTP 200 at `192.168.2.5:8080`. The current-day CSV was 102,429,441 bytes, +1,506,420 over the supplied 12:35:42 sample. The final 12:59:02+01:00 sample retained the same process/task/endpoint proof and measured 103,246,180 bytes: +816,739 from AF's first sample and +2,323,159 from the supplied before sample. Every operation-tree access was read-only; no task, settings, release, rollback, log, archive, manifest, or runtime write occurred. |
| 10 | The campaign plan and history contain criterion-specific evidence with ledger state implemented but not automatically accepted; Phase 3 characterization is closed, the backlog advances to Plan-008, and live-tracker records all four lanes. | met | `data/plans/plan-007.json` and this table agree on all ten criteria and record `implemented`, never `accepted`. The roadmap marks Phase 3 complete without starting Phase 4, the backlog removes Plan-007 and advances to Plan-008 with next letter `ag`, A1 remains open and person-only, and `live-tracker.md` records AC, AD, AE, and AF. |

## plan-008 - Immutable sensor snapshot and data.json projection

The first Phase-4 seam copies the mutable WinForms Node hierarchy under its
existing structural lock into a detached immutable snapshot, then projects that
snapshot through a pure ordered `data.json` adapter. The tracked plan lifecycle
remains `executed`; the ledger state is `implemented` with all ten
criteria met. It is not `accepted`: that transition remains person-only. The
campaign produced source and verification evidence only and did not create a
candidate or change live state.

| # | Criterion | State | Evidence |
| --- | --- | --- | --- |
| 1 | Outside campaign/configuration documentation, the integrated diff is limited to six exclusively owned files: two new internal snapshot/capture sources, one new Application test file, one new pure projection source, HttpServer.cs, and one new Contracts test file; no MainForm, library, project, package, solution, golden, spike, web, operations, release, or live-runtime file changes. | met | `git diff --name-status 59a0c37968c9837b43b8baa17cad4ca5762dde80..3a31eb59039a4ce51be642534d8ac9458e9441fd` lists exactly the six AG/AH paths: three snapshot/capture/Application files and three projection/HttpServer/Contracts files. The product baseline is `779d9044ef63b3cb91f547d9306dc543e2eb4ae6`; the corrected planning baseline is `59a0c37968c9837b43b8baa17cad4ca5762dde80`. No prohibited path appears. |
| 2 | AG adds ordinary sealed net472-compatible snapshot/value/node types whose children are defensively copied and read-only and whose retained values contain no Node, HardwareNode, SensorNode, IHardware, ISensor, mutable collection, or Avalonia type reference. | met | Source inspection and the passing immutability/property-graph fact prove internal sealed `SensorSnapshot`, `SensorNodeSnapshot`, and `SensorValueSnapshot` types, a defensive array copy behind `ReadOnlyCollection`, and no retained source, hardware, sensor, control, delegate, image, or mutable supplied-list reference. The shipping scan found no Avalonia or `Spike.Core` reference. Both Release targets compiled the ordinary types, including net472, with 0 warnings and 0 errors. |
| 3 | AG captures the tree recursively under Node.SyncRoot in existing producer order, includes hidden nodes, preserves opaque case-sensitive identifiers, exact formatted/raw triples, kinds and legacy image URLs, releases the lock before returning, documents that capture is detached rather than an atomic all-sensor sample, and adds exactly five passing Application facts. | met | AG source `dbc7e5dd20b49ceba1f5827d4e2cb3e31158a856` integrated as `cbd1013c431cb745c6f30420755491963197c843`, stable patch-id `9b8bb96abbaffabc9fb75940c91f7deff9029345`, with all three owned blobs identical. `SensorSnapshotTests` passed 5/5 and cover lock-held read order, recursive producer order, hidden nodes, case-sensitive IDs, exact display/raw triples, legacy images, detachment, and read-only children. The full Application result was 152 discovered, 151 passed, and the one established opt-in skip. |
| 4 | AH adds a pure projector that assigns envelope id zero and consecutive depth-first preorder node ids, recreates the exact existing root and node property insertion order and optional-field presence, preserves display strings and finite boxed float values, maps non-finite raw readings to null, is deterministic without mutating the snapshot, and adds exactly three passing Contracts facts. | met | AH source `215190f93a7494331a5d534e66fbff2b516f120d` integrated as `3a31eb59039a4ce51be642534d8ac9458e9441fd`, stable patch-id `a00a0babf9417a688fe5483d6fba0c644bdd5d24`, with all three owned blobs identical. `DataJsonProjectionTests` passed 3/3 and pin the envelope/preorder IDs, exact key order and optional fields, legacy strings/images, finite boxed floats, non-finite null mapping, repeatable bytes, and unchanged snapshot references/values. |
| 5 | HttpServer.BuildDataJsonObject and WriteDataJson retain their signatures; only the data.json tree-walk is replaced by capture then projection, while Sensor and Prometheus traversal, listener dispatch, headers, buffer gate, pooled copy, gzip, cancellation, response closure, and MainForm construction remain unchanged and serialization/I/O occur outside Node.SyncRoot. | met | Diff and full-file review show `BuildDataJsonObject()` now calls `WinFormsNodeSensorSnapshotSource.Capture(_root)` and then `DataJsonProjection.Project(snapshot, _version)`; `WriteDataJson(Stream)` retains its signature and serialization call. The capture adapter owns the lock and returns before projection/serialization. All other request, response, buffering, gzip, cancellation, Sensor, Prometheus, and construction paths are unchanged. The separate Sensor-plus-Prometheus filter passed 47/47; the prescribed golden/Sensor/Prometheus combined filter passed 49/49. |
| 6 | DataJsonGoldenTests.cs and data.golden.json remain byte-for-byte unchanged; both existing golden facts pass and the golden file retains Git blob 05113704acc6fefeb4128004b3f523d876fbcec4 and SHA-256 BEBDE807A7F0037827E16CFBC1F41707701F2BEE7232385C3D2EAEBD2261D2F3. | met | `git diff --exit-code 779d9044ef63b3cb91f547d9306dc543e2eb4ae6..HEAD` over the golden source and file exited 0. `git hash-object` returned `05113704acc6fefeb4128004b3f523d876fbcec4`, SHA-256 returned `BEBDE807A7F0037827E16CFBC1F41707701F2BEE7232385C3D2EAEBD2261D2F3`, and `DataJsonGoldenTests` passed 2/2 without regeneration. |
| 7 | The deterministic solution discovers exactly 287 cases with 286 passed and exactly the existing SettingsPersistenceTests.LiveConfigCopy_LoadsAndCompactsWithinMemoryBudgets opt-in skip; all eight new facts pass and no baseline fact is lost, duplicated, or newly skipped. | met | With both live-config variables unset and terminal logging disabled, the independent solution-filter run produced Library 68/68, Contracts 67/67, and Application 152 discovered/151 passed/one skipped. The exact aggregate is 287 discovered, 286 passed, and one skip; the only skip is `SettingsPersistenceTests.LiveConfigCopy_LoadsAndCompactsWithinMemoryBudgets`. The AG 5/5 and AH 3/3 focused runs prove all eight new facts pass. |
| 8 | The focused snapshot/projection/data.json tests, complete deterministic solution, both WinForms x64 Release framework builds, 75-test isolated Avalonia spike gate, web tests, and all eight included non-deploying CI gates pass with no new warnings or errors. | met | Independent focused and aggregate .NET results are recorded above. Direct WinForms x64 Release `net10.0-windows` and `net472` builds each passed with 0 warnings/0 errors. The isolated spike passed 75/75 with its established `AVLN3001` warning only. Web checks passed 315/315 and 18/18. The final `Invoke-LhmGates.ps1 -All` sweep passed all 8 included gates; `ci-gates` was excluded only because it invokes the runner itself. |
| 9 | Exclusive worktree ownership is preserved, implementation commits are integrated dependency-first with explicit file and patch checks, protected/unrelated dirty state is neither merged nor removed, and final campaign worktrees/branches and reproducible build outputs are guard-cleaned only after integration proof. | met | The coordinator supplied a clean AI worktree at integrated `3a31eb5`. AG `cbd1013` is an ancestor of AH `3a31eb5`; the source/integrated stable patch IDs and all six owned-file blob pairs match. AI changed only its exclusive campaign/configuration/documentation outputs. No merge helper, branch/worktree removal, ignored-state deletion, or cleanup ran in this lane; any later exact-path guarded cleanup remains coordinator-owned and can only occur after this integration proof. |
| 10 | Read-only SND-HOST proof confirms the live deployment remains separate and healthy; campaign truth records criterion-specific implemented evidence without automatic acceptance, Phase 4 item 1 is complete and Phase 4 is in progress, the backlog advances to Plan-009 and agent aj, stale data.json/roadmap guidance is corrected, and A1 remains open and person-only. | met | Identity was `VERIFIED` as `snd-host`. AI samples at 15:24:44+01:00 and 15:30:34+01:00 retained exactly one PID 14876 at the declared live executable, root task `\LibreHardwareMonitor` `Running`/267009 with exact action and working directory, and HTTP 200 for `/`, `/data.json`, and `/metrics` on `192.168.2.5:8080`. The current-day CSV grew from 118,509,168 to 119,129,453 bytes (+620,285; +9,180,795 from the supplied 109,948,658-byte sample). No operational write occurred. The plan remains `executed`, this ledger says `implemented`, Phase 4 item 1 is complete/in progress, Plan-009 and `aj` are next, and A1 remains open and person-only. |

## Waiver records

No waiver records exist.

A waiver is the only way a criterion may be treated as closed without being met.
Every waiver needs all five fields below; a waiver missing any field is treated
as `open` by `scripts/task_runtime/test_campaign_history.py`.

| Plan | Criterion | Waived by | Date | Reason | Accepted risk | Reopens if |
| --- | --- | --- | --- | --- | --- | --- |
